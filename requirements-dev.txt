# Test / dev-only dependencies.
# Kept OUT of requirements.txt so the runtime image and `pip-audit -r requirements.txt`
# (the CI security gate) aren't burdened with test tooling.
#
# Install for local dev / CI:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# pytest >= 9.0.3 clears CVE-2025-71176 (local /tmp DoS) — that's why the CI
# pip-audit step no longer needs to ignore it.
pytest>=9.0.3,<10
pytest-mock>=3.14,<4
pytest-asyncio>=1.0,<2
pytest-cov>=6,<7
fakeredis>=2.20,<3
httpx>=0.27,<1
